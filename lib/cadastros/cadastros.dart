import 'package:flutter/material.dart';

class Cadastros extends StatelessWidget {
  const Cadastros({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Cadastros'));
  }
}
