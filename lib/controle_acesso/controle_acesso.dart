import 'package:flutter/material.dart';

class ControleAcesso extends StatelessWidget {
  const ControleAcesso({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Controle de acesso'));
  }
}
