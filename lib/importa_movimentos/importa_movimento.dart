import 'package:flutter/material.dart';

class ImportaMovimento extends StatelessWidget {
  const ImportaMovimento({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Importa Movimento'));
  }
}
